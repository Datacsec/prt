# =============================================================
# application.properties - portail-datalake
# =============================================================

server.port=8080

# ---------------------------------------------------------------
# SQLite - Remplace tout fichier texte pour les utilisateurs
# users.db créé automatiquement à la racine du projet au démarrage
# ---------------------------------------------------------------
spring.datasource.url=jdbc:sqlite:./users.db
spring.datasource.username=
spring.datasource.password=
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ---------------------------------------------------------------
# SAML2 - WebSSO ITG Staging (environnement intégration)
# ---------------------------------------------------------------

# Identifiant de votre application côté IdP
spring.security.saml2.relyingparty.registration.websso-itg.entity-id=https://votre-app-itg.echonet/saml/metadata

# URL de callback après authentification
spring.security.saml2.relyingparty.registration.websso-itg.acs.location=http://localhost:8080/login/saml2/sso/websso-itg

# Metadata IdP - Option A : URL directe (si réseau accessible)
spring.security.saml2.relyingparty.registration.websso-itg.assertingparty.metadata-uri=https://websso-itg.staging.echonet/metadata/a1metadata.xml

# Metadata IdP - Option B : fichier local (décommenter si URL non accessible)
# spring.security.saml2.relyingparty.registration.websso-itg.assertingparty.metadata-uri=classpath:saml/stagingmetadata-14_11_2033.xml

# Logout SAML
spring.security.saml2.relyingparty.registration.websso-itg.singlelogout.binding=POST
spring.security.saml2.relyingparty.registration.websso-itg.singlelogout.response-url={baseUrl}/logout/saml2/slo

# ---------------------------------------------------------------
# Logs SAML (debug - à désactiver en prod)
# ---------------------------------------------------------------
logging.level.org.springframework.security.saml2=DEBUG
logging.level.org.opensaml=DEBUG
